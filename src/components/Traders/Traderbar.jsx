/** @module Traderbar */

import React from "react";

const Traderbar = (props) => {
  //   return traderNames.map((name) => <TraderButton traderName={name} />);
  return <p>hi</p>;
};

export default Traderbar;
